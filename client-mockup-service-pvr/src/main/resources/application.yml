spring:
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/client_pvr
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
      
server:
  servlet:
    context-path: /api/pvr
  port: 9091
