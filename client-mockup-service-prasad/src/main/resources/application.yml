spring:
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/client_prasad
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
      
server:
  servlet:
    context-path: /api/prasad
  port: 9092
